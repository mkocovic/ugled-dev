/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'via.placeholder.com'], // add any other external image domains here
    },
  };
  
  export default nextConfig;
  